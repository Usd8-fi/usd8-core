[profile.default]
src = "src"
out = "out"
libs = ["lib"]
optimizer = true
# DefiInsurance is close to EIP-170; runs=1 keeps upgrade bytecode deployable
# without via-IR, whose pipeline changes produced lifecycle test regressions.
optimizer_runs = 1
remappings = [
    "@openzeppelin/contracts/=lib/openzeppelin-contracts/contracts/",
    "@openzeppelin/contracts-upgradeable/=lib/openzeppelin-contracts-upgradeable/contracts/",
    "forge-std/=lib/forge-std/src/",
    "vault-v2/=lib/vault-v2/",
]

# Long-running campaign used by the scheduled deep-fuzz workflow. Keep the
# default profile fast enough for local development and pull requests.
[profile.deep.fuzz]
runs = 10000
show_edge_coverage = true

[profile.deep.invariant]
runs = 1000
depth = 1000
fail_on_revert = true
show_metrics = true

# Kontrol consumes this compiler metadata and requires Cancun for TLOAD/TSTORE.
[profile.kontrol]
test = "formal-verification"
out = "formal-verification/out"
cache_path = "formal-verification/cache"
dynamic_test_linking = false
solc_version = "0.8.28"
evm_version = "cancun"
extra_output = ["storageLayout", "abi", "evm.methodIdentifiers", "evm.deployedBytecode.object", "devdoc"]
